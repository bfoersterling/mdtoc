name: Build and Push Docker Images

on:
  schedule:
    - cron: '0 12 * * 1'  # Weekly build (every Monday at 12:00 UTC)
  workflow_dispatch:  # Allow manual trigger

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    permissions:
      packages: write # Required for GHCR
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry (GHCR)
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "$GITHUB_ACTOR" --password-stdin

      - name: Build Alpine Image
        run: |
          docker build -t ghcr.io/${{ github.repository_owner }}/cdevel_alpine:latest \
            -f .docker/Dockerfile_alpine .

      - name: Push Docker image
        run: |
          docker push ghcr.io/${{ github.repository_owner }}/cdevel_alpine:latest

      - name: Build Archlinux Image
        run: |
          docker build -t ghcr.io/${{ github.repository_owner }}/cdevel_archlinux:latest \
            -f .docker/Dockerfile_archlinux .

      - name: Push Docker image
        run: |
          docker push ghcr.io/${{ github.repository_owner }}/cdevel_archlinux:latest
